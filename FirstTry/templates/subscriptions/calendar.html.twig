{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path ('event_subscription', {id:event.id} ) }}">Create new booking</a>

    <div
        id="calendar-holder"
        data-events-url="{{ path('event', {id:event.id}) }}"
    ></div>
Voici les données reçues du controller pour afficher dans le calendrier 

<div id="calendrier" data-calendrier="{{ evenementsJSON }}" class="calendar"></div>

{{ dump(evenementsJSON) }}

    {% block stylesheets %}
        {{ parent() }}
        {{ encore_entry_link_tags('calendar') }}
    {% endblock %}

    {% block javascripts %}
        {{ parent() }}
        {{ encore_entry_script_tags('calendar') }}
    {% endblock %}
{% endblock %}

